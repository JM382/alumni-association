# 需求说明
目标用户
核心用户：九校一院全体校友（在读学生、已毕业校友、退休校友）
衍生用户：校友企业、合作机构（福利商户、捐赠接收方）
管理用户：一级管理员、二级管理员（会员管理 / 企业管理 / 经费管理）
游客用户：未注册 / 未认证用户（限浏览公开内容）

注册类型与用户角色
游客用户：未注册 / 未认证用户（限浏览公开内容）
校友注册：需选择所属院校（九校一院下拉选择）、填写姓名、身份证号 / 学号、入学年份、毕业院系 / 专业、学历、联系电话、邮箱、居住区域、当前所在单位 / 职务。
企业注册：需填写企业名称、所属行业、统一社会信用代码、企业地址、联系人姓名、联系电话、邮箱、企业简介、企业 logo 等。
管理员：一级管理员、二级管理员（会员管理 / 企业管理 / 经费管理/专家管理），由校友会指定账号，负责会员管理、企业管理、经费管理等后台功能。

整体导航结构（底部 TabBar）
导航名称    核心定位
首页        首页核心入口，聚合动态与快捷功能
校友服务    服务福利、返校、捐赠等核心服务
资讯        新闻资讯分类列表（母校新闻、校友会通知、行业动态、校友故事、政策资讯）
校友圈      社群社交互动、活动报名、校友查找
我的        我的个人信息、订单、权益管理
一、导航 1：首页界面展示内容
1.顶部 Banner 区：轮播展示母校最新动态、校友活动预告、校园风光、合作福利推广（如酒店 / 航空优惠）。
2.快捷功能入口区（8 个核心入口，图标 + 文字）：o电子校友卡、返校预约、校友福利、捐赠通道o校友企业、校友联谊、校友资源、专家资源
3.动态推送区（滚动列表）：o分类展示：校友故事、母校新闻、行业动态、校友企业招聘、活动预告o每条动态包含：标题、缩略图、发布时间、浏览量
二、导航 2：校友服务界面展示内容
1.服务分类标签（顶部横向滑动）：o校友福利、返校服务、捐赠通道、其他服务
2.各分类对应内容：o【校友福利】（默认展示）：卡片式展示所有合作福利（酒店特权、顺丰优惠、瑞幸咖啡、超星图书馆、一嗨租车、南航优惠）每张卡片包含：福利名称、核心权益、合作方 Logo、“立即使用” 按钮（跳转至对应小程序 / 页面）o【返校服务】：预约入校：填写姓名、校友卡号、预计到访时间、同行人数的表单校园导览：电子地图（标注教学楼、食堂、纪念馆、招待所位置）住宿推荐：校内招待所预订入口、合作酒店优惠跳转o【捐赠通道】：捐赠项目分类：助学基金、校园建设、科研支持（带项目介绍 + 捐赠目标进度）捐赠金额选择：固定金额（50/100/200 元）+ 自定义金额支付方式：微信支付、支付宝、银联（图标展示）捐赠后即时生成电子版捐赠证书（可保存 / 分享）
三、导航 3：校友圈界面展示内容
核心内容区：
o【动态广场】：
  - 发布按钮：
  - 发布动态：支持图文 / 视频上传，支持添加话题标签（如 #校友联谊、#行业交流、#校园回忆）
  - 后续可扩展发布活动、发布资源信息等入口
  - 展示校友发布的图文 / 视频动态，默认按发布时间倒序排序。
  - 每条动态包含：发布者头像、姓名、年级专业、发布时间、内容、点赞 / 评论 / 分享按钮。
  - 支持话题筛选（如 #校友联谊、#行业交流 等）。
o【活动报名】：
  - 活动列表：线上讲座、线下聚会、返校日活动、行业交流活动等。
  - 每条活动包含：活动类型、活动主题、时间、地点、已报名人数 / 名额、“立即报名” 按钮。
  - 点击活动进入详情页，可查看详细介绍并完成报名（报名信息记录到“我的活动”）。
o【校友查找】：
  - 搜索框：按年级、专业、地区、姓名查找校友（支持组合筛选）
  - 筛选条件：年级、专业、地区、行业等（可组合筛选）。
  - 校友列表：头像、姓名、年级专业、所在地区 / 企业，支持发起私信或添加为通讯录好友（需登录并通过认证后使用）。

四、导航 4：我的界面展示内容
1.个人信息卡片（顶部醒目区域）：
o 展示内容：头像、姓名、校友卡号（No.XXXX）、入学年份、所属学院等核心信息。
o 身份标识：显示“已认证 / 未认证”等状态标签。
o 电子校友卡入口：点击可查看完整电子校友卡（支持展示二维码、分享）。

2.核心功能入口区（宫格功能区）：
o 卡包：已激活的校友卡、合作福利会员卡列表。
o 积分：积分余额展示，查看积分获取记录、积分兑换入口。
o 通讯录：已添加的校友、我加入的组织（如院系分会、地区分会）。
o 我的活动：我报名参加的活动、我发起 / 管理的活动（组织者视角）。
o 我的订单：酒店、机票、租车、快递等消费记录列表（可跳转到详情 / 合作方小程序）。
o 我的捐赠：历史捐赠记录、捐赠证书收藏（证书可查看大图 / 下载 / 分享）。
o 找人脉：快速跳转到校友查找页面，基于我的行业和地区优先推荐。
o 资源求助 / 资源发布：进入资源板块，发布 / 查看求助信息、合作信息等。
o 个人信息：进入个人资料详情页，可编辑基础信息、教育背景、职业信息以及隐私设置。

3.设置入口（底部）：
o 账号设置：修改绑定手机号、登录方式、注销账号申请等。
o 消息通知：活动通知、系统公告、互动消息的通知开关设置。
o 隐私设置：控制谁可以搜索到我、哪些个人信息对他人可见等。
o 帮助中心：常见问题、使用指南。
o 关于我们：校友会简介、联系方式（邮箱、电话等）。
五、资讯
3.1 新闻分类分类列表：母校新闻、校友会通知、行业动态、校友故事、政策资讯交互逻辑：顶部 Tab 切换分类，默认显示 “全部新闻”
3.2 新闻详情页展示内容：标题、发布时间、来源、正文（图文结合）、相关新闻推荐交互功能：支持点赞、收藏、分享（微信 / 朋友圈 / QQ）、评论（需登录会员账号）设计要求：正文文字字号 16px，行间距 1.5 倍，图片自适应屏幕宽度
3.3 新闻发布（管理员 / 会员权限）管理员发布：支持发布所有分类新闻，可上传图片 / 视频、设置置顶 / 推荐、定时发布会员发布：仅支持发布 “校友故事” 分类内容，需填写标题、正文、上传图片，提交后经管理员审核方可发布交互逻辑：发布页面提供富文本编辑器（支持加粗、换行、插入图片），发布成功后跳转至新闻

六、校友圈接口设计（数据库与云函数）

1. 数据库集合与字段设计

1.1 动态相关

- 集合：`circle_posts`（校友圈动态）
  - `userId`：string，发帖人 users.user_id，仅校友（identity='alumni'）可发
  - `openid`：string，发帖人 openid
  - `content`：string，动态文本内容
  - `images`：string[]，图片 fileID 列表（0~9 张）
  - `createdAt`：Date，创建时间
  - `updatedAt`：Date，更新时间
  - `visible`：boolean，是否可见（软删除用，默认 true）
  - `likeCount`：number，点赞数（冗余字段）
  - `commentCount`：number，评论数（冗余字段）
  - `location`：string，可选，地理位置或地点说明
  - `onlyAlumni`：boolean，仅校友可见开关（预留，默认 false）
  - `extra`：object，扩展字段（如话题标签等）

- 集合：`circle_comments`（动态评论/回复）
  - `postId`：string，对应 circle_posts._id
  - `userId`：string，评论人 users.user_id
  - `openid`：string
  - `content`：string，评论内容
  - `parentId`：string|null，顶级评论为 null，回复评论为被回复评论的 _id（只做两级）
  - `createdAt`：Date
  - `visible`：boolean，是否可见（软删除）
  - `extra`：object，扩展字段

- 集合：`circle_likes`（动态点赞记录）
  - `postId`：string，被点赞动态 circle_posts._id
  - `userId`：string，点赞人 users.user_id
  - `openid`：string
  - `createdAt`：Date
  - 索引建议：唯一索引 { postId: 1, userId: 1 }，防止重复点赞

1.2 互动消息相关

- 集合：`notifications`（互动通知）
  - `userId`：string，接收通知的人（被点赞 / 被评论者）
  - `type`：string，枚举：'like' | 'comment' | 'reply' | 'system' 等
  - `fromUserId`：string，触发人 users.user_id（点赞 / 评论 / 回复者）
  - `postId`：string|null，关联动态 circle_posts._id（点赞 / 评论）
  - `commentId`：string|null，关联评论 circle_comments._id（评论 / 回复）
  - `contentSnapshot`：string，用于通知卡片展示的简要文案（例如“张三评论了你的动态：……”）
  - `isRead`：boolean，是否已读
  - `createdAt`：Date
  - `extra`：object，扩展字段（如跳转参数）
  - 说明：无论用户的互动消息开关 notification_settings.interactOn 是否打开，这里始终写入记录；开关只控制是否做前端即时提醒。

1.3 活动相关

- 集合：`events`（校友圈活动）
  - `creatorUserId`：string，创建人 users.user_id（必须为校友 identity='alumni'）
  - `title`：string，活动标题
  - `type`：string，'online' | 'offline'（线上会议 / 线下活动）
  - `coverImage`：string，封面图 fileID
  - `brief`：string，简要介绍
  - `content`：string，详细介绍
  - `startTime`：Date，开始时间
  - `endTime`：Date，结束时间
  - `location`：string，线下：具体地址；线上：会议链接 / 平台说明
  - `maxParticipants`：number|null，最大报名人数（null 或 0 表示不限）
  - `status`：string，'draft' | 'published' | 'ended' | 'canceled'
  - `participantCount`：number，当前报名人数（冗余字段）
  - `tags`：string[]，如 ['线上会议', '校友聚会']
  - `createdAt`：Date
  - `updatedAt`：Date
  - `extra`：object，扩展字段

- 集合：`event_signups`（活动报名记录）
  - `eventId`：string，对应 events._id
  - `userId`：string，报名人 users.user_id
  - `openid`：string
  - `status`：string，'signed' | 'canceled' | 'checked_in'
  - `createdAt`：Date
  - `updatedAt`：Date
  - `extra`：object，扩展字段（如报名表单信息：手机号、备注等）

1.4 校友查找（复用 users 集合）

- 在现有 users 集合中，确保字段：
  - `identity`：'visitor' | 'alumni' | 'company' | 'expert'
  - `schoolName`：string，学校名称
  - `major`：string，专业
  - `enterYear`：number，入学年份（建议在校友认证通过时写回）
  - 其他展示信息：`nickname`、`avatarUrl`、`graduationYear` 等

2. 云函数接口设计

2.1 校友圈动态云函数：circle

- action: `createPost`（发布动态）
  - 入参：
    - `content`: string，文本内容
    - `images`: string[]，图片 fileID 列表
  - 权限：
    - 必须登录，且 users.identity === 'alumni'（仅校友可发动态）
  - 逻辑：
    - 校验内容非空，图片数量不超过上限（如 9 张）
    - 向 circle_posts 插入一条记录（likeCount=0, commentCount=0, visible=true）
  - 返回：
    - `success`: boolean
    - `postId`: string，新动态 _id

- action: `listPosts`（动态列表）
  - 入参：
    - `page`: number，页码，从 1 开始
    - `pageSize`: number，每页数量（默认 10~20）
  - 权限：
    - 必须登录（任意身份），用于获取 userId 判断自己是否点赞
  - 逻辑：
    - 按 createdAt 倒序查询 circle_posts，条件：visible=true
    - 分页返回，附带 likeCount / commentCount
    - 可选：查询当前用户在 circle_likes 中的记录，附加 `hasLiked` 字段到每条动态
  - 返回：
    - `list`: 动态数组
    - `total`: 总数量（可选）

- action: `getPostDetail`（动态详情 + 评论）
  - 入参：
    - `postId`: string
  - 逻辑：
    - 查询 circle_posts._id=postId
    - 查询 circle_comments：
      - 顶级评论：parentId == null
      - 回复评论：parentId == 顶级评论 _id
    - 组装为：post + comments（每条评论带 replies 数组）
  - 返回：
    - `post`: 动态详情
    - `comments`: 评论及回复列表

- action: `toggleLike`（点赞 / 取消点赞）
  - 入参：
    - `postId`: string
  - 权限：
    - 必须登录
  - 逻辑：
    - 在 circle_likes 查 { postId, userId }：
      - 若不存在：插入新记录，circle_posts.likeCount +1
        - 同时生成一条 notifications 记录：
          - `userId`: 动态作者 userId
          - `fromUserId`: 当前用户
          - `type`: 'like'
          - `postId`: 当前动态
          - `contentSnapshot`: 简要文案（例如“某某点赞了你的动态”）
      - 若已存在：删除此记录，circle_posts.likeCount -1
  - 返回：
    - `liked`: boolean，当前操作后是否为已点赞状态

- action: `addComment`（发表评论 / 回复）
  - 入参：
    - `postId`: string
    - `content`: string
    - `parentId?`: string，可选，回复某条评论时传该评论 _id
  - 权限：
    - 必须登录
  - 逻辑：
    - 插入 circle_comments（带 postId, parentId, userId 等）
    - circle_posts.commentCount +1
    - 生成 notifications：
      - 顶级评论：通知给动态作者 post.userId，type='comment'
      - 回复评论：通知给被回复评论的 comment.userId（如果不是自己），type='reply'
      - contentSnapshot 填写评论内容截断
  - 返回：
    - `commentId`: 新评论 _id

2.2 互动消息云函数：notifications

- action: `listInteractions`（互动消息列表）
  - 入参：
    - `page`: number
    - `pageSize`: number
  - 权限：
    - 必须登录
  - 逻辑：
    - 查询 notifications：
      - `userId` 为当前用户
      - `type` in ['like', 'comment', 'reply']
    - 按 createdAt 倒序分页
  - 返回：
    - `list`: 通知数组（含 type, fromUserId, postId, commentId, contentSnapshot, isRead, createdAt）
    - `total`: 总数量（可选）

- action: `markAsRead`（标记已读）
  - 入参：
    - `notificationId?`: string，单条通知 id；若为 'all' 或不传，则标记当前用户全部互动通知为已读
  - 权限：
    - 必须登录
  - 逻辑：
    - 单条：更新 { _id: notificationId, userId: 当前用户 } 的 isRead=true
    - 全部：更新 { userId: 当前用户, type in ['like','comment','reply'] } 的 isRead=true
  - 返回：
    - `success`: boolean

2.3 活动云函数：events

- action: `createEvent`（创建活动）
  - 入参：
    - `title`, `type`, `coverImage`, `brief`, `content`, `startTime`, `endTime`, `location`, `maxParticipants`, `tags`
  - 权限：
    - 必须登录，且 users.identity === 'alumni'（仅校友可发布活动）
  - 逻辑：
    - 校验字段完整性和时间合法性
    - 插入 events，初始：
      - `status: 'published'`
      - `participantCount: 0`
      - `creatorUserId`: 当前用户 user_id
  - 返回：
    - `eventId`: 新活动 _id

- action: `listEvents`（活动列表）
  - 入参：
    - `status?`: string，可选过滤状态（如 'published'）
    - `type?`: string，可选 'online' | 'offline'
    - `page`, `pageSize`
  - 逻辑：
    - 在 events 上按条件构造 where
    - 按 startTime 或 createdAt 倒序分页返回
  - 返回：
    - `list`: 活动数组（含基础信息和 participantCount）

- action: `getEventDetail`（活动详情）
  - 入参：
    - `eventId`: string
  - 权限：
    - 必须登录（用于判断当前报名状态）
  - 逻辑：
    - 查询单条 events
    - 查询 event_signups 中是否有当前用户 + eventId 的记录，返回 hasSigned
  - 返回：
    - `event`: 活动详情
    - `hasSigned`: boolean

- action: `signupEvent`（报名活动）
  - 入参：
    - `eventId`: string
  - 权限：
    - 必须登录；（如需仅校友报名，可再校验 identity）
  - 逻辑：
    - 检查 events.status === 'published'
    - 若 maxParticipants > 0 且 participantCount >= maxParticipants，返回“名额已满”
    - 在 event_signups 查 { eventId, userId }，若已存在且 status='signed'，返回“已报名”
    - 否则插入报名记录，status='signed'，并令 events.participantCount +1
  - 返回：
    - `success`: boolean

- action: `cancelSignup`（取消报名）
  - 入参：
    - `eventId`: string
  - 权限：
    - 必须登录
  - 逻辑：
    - 查找 event_signups 记录，若存在：
      - 将 status 设置为 'canceled'
      - events.participantCount -1（不小于 0）
  - 返回：
    - `success`: boolean

2.4 校友查找云函数：alumniSearch

- action: `search`（校友搜索）
  - 入参：
    - `schoolName?`: string，学校
    - `major?`: string，专业
    - `enterYear?`: number，入学年份
    - `page`, `pageSize`
  - 权限：
    - 至少登录（可根据需求限制仅校友 / 会员使用）
  - 逻辑：
    - 在 users 集合中 where：
      - `identity: 'alumni'`
      - 若传 schoolName，则加 `schoolName` 过滤
      - 若传 major，则加 `major` 过滤
      - 若传 enterYear，则加 `enterYear` 过滤
    - 按创建时间或入学年份等排序分页返回
  - 返回：
    - `list`: 校友数组（字段：nickname, avatarUrl, schoolName, major, enterYear, graduationYear 等）
    - `total`: 总数量（可选）

七、社交/私信模块接口设计（数据库与云函数）

1. 数据库集合与字段设计

1.1 关注关系

- 集合：`follows`（关注关系）
  - `fromUserId`：string，关注者 users.user_id
  - `toUserId`：string，被关注者 users.user_id
  - `status`：string，'active'（正在关注）| 'cancelled'（已取消，保留历史）
  - `createdAt`：Date
  - `updatedAt`：Date
  - 说明：互相关注即“好友”，通讯录展示好友列表。建议唯一索引 { fromUserId: 1, toUserId: 1 }。

1.2 会话

- 集合：`conversations`（私信会话）
  - `type`：string，'private'（一对一，后续可扩展 'group'）
  - `userIds`：string[]，参与人 user_id 列表，一对一为 [minUserId, maxUserId] 升序
  - `lastMessageText`：string，最后一条消息摘要（列表展示用）
  - `lastMessageAt`：Date
  - `lastMessageSenderId`：string
  - `unreadMap`：object，各用户在该会话的未读数，如 { "user_id_1": 0, "user_id_2": 3 }
  - `createdAt`：Date
  - `updatedAt`：Date
  - 说明：同一对用户仅一条 type='private' 会话，由 ensureConversation 保证。

1.3 消息

- 集合：`messages`（私信消息）
  - `conversationId`：string，对应 conversations._id
  - `fromUserId`：string，发送人
  - `toUserId`：string，接收人
  - `contentType`：string，'text' | 'image' | 'card' | 'system'
  - `text`：string，文本内容
  - `media`：string[]，图片/文件 fileID 列表（后续扩展）
  - `extra`：object，扩展字段
  - `createdAt`：Date
  - `status`：string，'normal' | 'revoked' | 'deleted'

1.4 拉黑（可选预留）

- 集合：`blocks`（拉黑，后续需要再实现逻辑）
  - `fromUserId`：string，拉黑操作人
  - `blockedUserId`：string，被拉黑用户
  - `createdAt`：Date

2. 云函数接口设计：social

所有接口均需登录，通过 OPENID 解析出当前 users.user_id。

- action: `toggleFollow`（关注/取关）
  - 入参：`targetUserId`，对方 user_id
  - 逻辑：不允许关注自己。若已有 fromUserId→toUserId 且 status='active' 则改为 'cancelled'；若为 'cancelled' 则改为 'active'；若无记录则新增 active。
  - 返回：`success`、`following`（当前是否关注）、`isMutual`（是否互关）

- action: `getFollowStatus`（查询关注状态）
  - 入参：`targetUserId`
  - 返回：`success`、`following`（我是否关注他）、`followedBy`（他是否关注我）、`isMutual`

- action: `ensureConversation`（获取或创建会话）
  - 入参：`targetUserId`
  - 逻辑：不能和自己会话。将两方 user_id 排序后查 conversations(type='private', userIds=[min,max])；不存在则新建一条，unreadMap 两人均为 0。
  - 返回：`success`、`conversationId`、`targetUser`（对方 nickname、avatarUrl、identity 等简要信息）

- action: `sendMessage`（发送私信）
  - 入参：`conversationId`、`toUserId`、`contentType`（默认 'text'）、`text`、`media`（可选）
  - 逻辑：校验当前用户在会话中；插入 messages；更新 conversations 的 lastMessage* 与 unreadMap[toUserId]+1
  - 返回：`success`、`message`（新消息对象）

- action: `listMessages`（消息历史）
  - 入参：`conversationId`、`page`、`pageSize`
  - 逻辑：校验当前用户在会话中；messages 按 createdAt 倒序分页
  - 返回：`success`、`list`

- action: `markRead`（标记已读）
  - 入参：`conversationId`
  - 逻辑：将会话中当前用户的 unreadMap[userId] 置 0
  - 返回：`success`

- action: `listConversations`（会话列表，类似微信聊天列表）
  - 入参：`page`、`pageSize`
  - 逻辑：查 conversations 中 userIds 包含当前用户的记录，按 lastMessageAt 倒序；每条附带对方 targetUser 与当前用户在该会话的 unread 数
  - 返回：`success`、`list`（含 conversationId、targetUser、lastMessageText、lastMessageAt、unread）

- action: `listContacts`（通讯录：互相关注好友）
  - 入参：`page`、`pageSize`
  - 逻辑：先查我关注且 status='active' 的 toUserId 列表；再查其中也关注了我（fromUserId=我、status='active'）的 fromUserId，得互关 id 列表；按该列表分页查 users 返回简要信息
  - 返回：`success`、`list`（userId、nickname、avatarUrl、schoolName、major、enterYear、city 等）